package API;

// TODO: exception, you should think about this, maybe try to do something earlier than in final method
// TODO : create method which will be save delete books to file, and you have to enter reason, why book has been deleted with library
public class Main {
    public static void main(String[] args) {
        MainApplication app = new MainApplication();
        app.startApp();

    }
}